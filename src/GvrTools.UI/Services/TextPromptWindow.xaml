<Window x:Class="GvrTools.UI.Services.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GVR Tools"
        Height="180" Width="420" MinHeight="180" MinWidth="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        FontFamily="Segoe UI" FontSize="13">

    <Window.Resources>
        <ResourceDictionary Source="pack://application:,,,/GvrTools.UI;component/Theme/GvrTheme.xaml"/>
    </Window.Resources>

    <Grid Margin="{StaticResource Gvr.Padding.Window}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageText" Grid.Row="0" Style="{StaticResource Gvr.Text.Label}"
                   TextWrapping="Wrap" Margin="0,0,0,10"/>

        <TextBox x:Name="ValueBox" Grid.Row="1" Style="{StaticResource Gvr.TextBox}"
                 KeyDown="ValueBox_KeyDown"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancelar" Style="{StaticResource Gvr.Button}" Width="90"
                    IsCancel="True" Click="Cancel_Click"/>
            <Button Content="Aceptar" Style="{StaticResource Gvr.Button.Action}" Width="90"
                    Margin="8,0,0,0" IsDefault="True" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
